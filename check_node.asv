function y = check_node(H, n, lk)

    y = zeros(1,size(H,2));

    for i = 1:size(H,2)
        
        if H(n,i) == 1
            
            y(i) = lk(i);
        end
        
    end

    

end

